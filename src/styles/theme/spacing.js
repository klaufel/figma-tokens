import {spacing} from '../../../tokens/json/spacing'

export default {
  xxs: spacing?.xxs?.value,
  xs: spacing?.xs?.value,
  sm: spacing?.sm?.value,
  md: spacing?.md?.value,
  lg: spacing?.lg?.value,
  xl: spacing?.xl?.value,
  xxl: spacing?.xxl?.value
}
